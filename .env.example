# Database password (used by compose for db + server/worker).
DB_PASSWORD=root

# Web UI admin login (optional). Set all three to enable login; leave unset to allow access without login.
# WEBUI_ADMIN_USER=admin
# WEBUI_ADMIN_PASSWORD=your-secure-password
# WEBUI_SECRET_KEY=generate-with-openssl-rand-hex-24
